const express = require("express");
const bodyParser = require("body-parser");
const cors = require("cors");
const regDB = require("./db/registerdb");

const app = express()
app.listen(3000)
const myPromise = new Promise()
